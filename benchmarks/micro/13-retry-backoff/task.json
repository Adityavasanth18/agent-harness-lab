{
  "id": "13-retry-backoff",
  "instruction": "Repair solution.py. delays(attempts,base,cap) produces attempts-1 retry delays, exponential base*2**i capped at cap. Reject attempts<1 or negative base/cap. Use only Python standard library. Do not change the public function signature.",
  "files": {
    "solution.py": "def delays(attempts, base, cap):\n    \"\"\"Repair this implementation to satisfy the task contract.\"\"\"\n    return None\n"
  },
  "solution": {
    "solution.py": "def delays(attempts, base, cap):\n    if attempts<1 or base<0 or cap<0: raise ValueError('configuration')\n    return [min(base*2**i,cap) for i in range(attempts-1)]\n"
  },
  "tests": {
    "test_solution.py": "import unittest\nfrom solution import delays\n\nclass ContractTests(unittest.TestCase):\n    def test_00(self):\n        self.assertEqual(delays(5,1,3),[1,2,3,3])\n    def test_01(self):\n        self.assertEqual(delays(1,2,4),[])\n    def test_02(self):\n        with self.assertRaises(ValueError): delays(0,1,2)\n\nif __name__ == '__main__':\n    unittest.main()\n"
  },
  "verify": [
    "python",
    "-m",
    "unittest",
    "-v",
    "test_solution"
  ]
}
