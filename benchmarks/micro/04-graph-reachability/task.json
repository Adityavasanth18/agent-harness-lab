{
  "id": "04-graph-reachability",
  "instruction": "Repair solution.py. reachable(graph,start) returns sorted reachable node names excluding start, handles cycles and missing adjacency keys. Use only Python standard library. Do not change the public function signature.",
  "files": {
    "solution.py": "def reachable(graph, start):\n    \"\"\"Repair this implementation to satisfy the task contract.\"\"\"\n    return None\n"
  },
  "solution": {
    "solution.py": "def reachable(graph, start):\n    seen={start}; todo=[start]\n    while todo:\n        for node in graph.get(todo.pop(),[]):\n            if node not in seen: seen.add(node); todo.append(node)\n    return sorted(seen-{start})\n"
  },
  "tests": {
    "test_solution.py": "import unittest\nfrom solution import reachable\n\nclass ContractTests(unittest.TestCase):\n    def test_00(self):\n        self.assertEqual(reachable({'a':['b'],'b':['a','c']},'a'), ['b','c'])\n    def test_01(self):\n        self.assertEqual(reachable({},'x'), [])\n    def test_02(self):\n        self.assertEqual(reachable({'a':['z','b'],'b':['z']},'a'), ['b','z'])\n\nif __name__ == '__main__':\n    unittest.main()\n"
  },
  "verify": [
    "python",
    "-m",
    "unittest",
    "-v",
    "test_solution"
  ]
}
