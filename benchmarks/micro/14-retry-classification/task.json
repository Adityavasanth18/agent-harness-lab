{
  "id": "14-retry-classification",
  "instruction": "Repair solution.py. retryable(status) is True only for HTTP 408,429,500,502,503,504. Use only Python standard library. Do not change the public function signature.",
  "files": {
    "solution.py": "def retryable(status):\n    \"\"\"Repair this implementation to satisfy the task contract.\"\"\"\n    return None\n"
  },
  "solution": {
    "solution.py": "def retryable(status):\n    return status in {408,429,500,502,503,504}\n"
  },
  "tests": {
    "test_solution.py": "import unittest\nfrom solution import retryable\n\nclass ContractTests(unittest.TestCase):\n    def test_00(self):\n        self.assertTrue(retryable(429)); self.assertTrue(retryable(503))\n    def test_01(self):\n        self.assertFalse(retryable(400)); self.assertFalse(retryable(501))\n    def test_02(self):\n        self.assertFalse(retryable(200))\n\nif __name__ == '__main__':\n    unittest.main()\n"
  },
  "verify": [
    "python",
    "-m",
    "unittest",
    "-v",
    "test_solution"
  ]
}
